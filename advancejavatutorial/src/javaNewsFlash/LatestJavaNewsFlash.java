package javaNewsFlash;

public class LatestJavaNewsFlash {

    /**
     * @FirstNewsFlash
     * Files.line is now used Memory Mapped Object.
     * what it means is that now we can perform parallel operation
     * on Files.lines
     * Files.lines().parallel().filler(l-> l.contains("Exception"))
     */
}
